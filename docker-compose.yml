services:
  client:
    tty: true
    build: .
    volumes:
      - .:/app:cached
      - node_modules:/app/node_modules
      - ~/.ssh:/root/.ssh
    ports:
      - 3000:3000
    command: npm run dev
    # command: npm run storybook

volumes:
  node_modules:
